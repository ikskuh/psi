const a = 10;
const b = 20;
const c = 30;

const d = a + b * c;

#assert -10;
#assert "Hallo!";
#assert enum(a,b,c);
#assert record(a : int, b : string);
#assert fn();
#assert fn() -> int;
#assert fn(x : int = 10,y:int) -> int;

// var x : int;
// var y : int;
/*
assert fn()
{
	var x : int;
	print("hi");
	var y : int;
};
*/