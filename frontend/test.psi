#assert -10;
#assert "Hallo!";

const foo = 1 < 2;
const bar = ~(2 < 1);

assert (foo & bar);

#assert record(a : int, b : string);
#assert fn();
#assert fn() -> int;
#assert fn(x : int = 10,y:int) -> int;

// var x : int;
// var y : int;
/*
assert fn()
{
	var x : int;
	print("hi");
	var y : int;
};
*/