/*****************************************************************************
 * Compiler Test Program                                                     *
 *****************************************************************************/

// tests dependencies
type bam = bar;

type baz = bam;

type foo = int;

type bar = foo;

// tests different types
type genders = enum(none,male,female,ah64);

type mainFunc = fn(argc : int, argv : array<string>) -> int;

type someType = type;

/*
// Tests out params
const set = fn(out dst : int, src : int) { dst = src; };

// Tests return values
const limit = fn() -> int { return 38; }

// Tests closures and flow control
const main = fn()
{
	var i : int;
	set(i, 42);
	
	while(i > limit())
	{
		i = i - 1;
	}

	const print = fn() 
	{
		print("i = ");
		print(i);
		print("\n");
	};

	print();
};
*/