program     := { <declaration> };

declaration := <vardecl> | <moddecl> | <assertion>;

moddecl     := "module" "{" <program> "}";

vardecl     := ("const" | "var") "identifier" [ ":" <type> ] [ "=" <expression> ] [ ";" ];

assertion   := "assert" <expression>;

type        := "identifier";

expression  := <product>;

product     := <sum> { ( "*" | "/" ) <sum> } ;

sum         := <value> { ( "+" | "-" ) <value> } ;

value       := <literal> | "identifier";

literal     := 'String' | 'Number';